#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @num1: First interger to be added
 * @num2: Second integer to be added
 *
 * Return: Always return the sum of @num1 and @num2
 */
int add(int num1, int num2)
{
	int sum = num1 + num2;

	return (sum);
}
