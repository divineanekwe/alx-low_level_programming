#include <stdio.h>
/**
 * main - Program starts with the entry point
 * puts - Write a line or string to the output(stdout) stream
 *
 * Description: Print exactly "Programming is like building a
 * multilingual puzzle
 * Return: Program should end with the value 0 (ends the program)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
